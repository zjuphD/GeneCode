# A-SUPPLY-001: pin the registry so local mirror configs cannot re-pollute
# package-lock.json with non-official URLs (mirror tarballs are content-
# identical but break supply-chain provenance tooling).
registry=https://registry.npmjs.org/
